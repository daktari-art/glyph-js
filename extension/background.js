// Background service worker for Glyph Debugger
console.log('🔮 Glyph Debugger background service worker loaded');

// No complex background logic needed for basic functionality
// The content script and DevTools panel communicate directly via window.postMessage
